# 🗂️ Experiment Log

## Rumi Locomotion
| WANDB ID | WANDB Exp Name | Date | Status | Objective | Key Results | What can be improved? |
|-----|-----|----|-----|---------|-------------|----------|
| 2026-02-21_20-33-09 | mjlab-rumi-locomotion | 2026-02-21 | ❌ **Failed** | First experiment after noticing the go1 structure. Removed actuator in xml, and hence the kp and kd for position actuator in MuJoCo are now set using stiffness and damping. Earlier we had the same stiffness and damping set to 1Hz natural frequency and at the same time had PD actuator in xml with kp of 10 and kd of 0, the policy was also successful. But now we have to re-train with a fixed natural frequency. Choosing a fixed natural frequency of 10Hz. The policy we trained earlier had 1Hz fixed but that calculation yielded some 0.196 as Kp which is 100 times lesser than actual value. | The training crashed, NaN value reached, rewards went negative and the robot also did not learn to walk. | I just noticed that I had accidentaly removed joint dampings too, I am going to add it back and run again |
| 2026-02-21_23-16-45 | mjlab-rumi-locomotion | 2026-02-21 | ✅ **Completed** | Continuing the previous run just by adding one damping parameter in the joint inside xml, but note that the xml still does not have any armature param, it is set using the `rumi_contants.py` | The robot did not learn to walk. To be more precise, it learned to rotate a bit, exploiting higher rewards for angular velocity achievement but failed to track linear velocity | The Kp that has resulted from the new 10Hz calculation is around like 19-20, which is twice of what is expected. One good way to run the training by actually setting `stiffness=10` & `damping=0` and then observing if it works |
| 2026-02-22_01-37-29 | mjlab-rumi-locomotion | 2026-02-22 | ✅ **Completed** | Trying to understand the effect of removing position actuator, since technically only kp and kd should have influenced, setting `stiffness=10` & `damping=0` for the run | It has seemed to learn properly | Easiest way, try to work the way back to get around `stiffness=12`, this can be done targeting natural frequency of nearly 7-8hz |
| 2026-02-22_04-37-34 | mjlab-rumi-locomotion | 2026-02-22 | ✅ **Completed** | Same as previous run but with 8Hz target natural frequency resulting in `stiffness~12` & `damping~1` | The robot still did not learn to walk | I personally think there is some issue when `stiffness` and `damping` are numerically float(ik it doesn't make sense but then hey I am not proven wrong yet), so why not put on a ceil/floor/round etc to like one decimal places? |
| 2026-02-22_04-44-21 | mjlab-rumi-locomotion | 2026-02-22 | ❌ **Failed** | Same as previous run but with 7Hz target natural frequency resulting in `stiffness~10` & `damping~0.5` | The run seemed to have exploded/hit NaN and stopped around 2k steps | Look at the prev run for inference |
| 2026-02-22_16-28-11 | mjlab-rumi-locomotion | 2026-02-22 | ✅ **Completed** | Trying the previous run with at 8Hz target natural frequency along with round off `stiffness` & `damping` to like nearest integer but still pass it as float(is it dumb, maybe but nothing is lost by experimenting tho!) | Although the robot did not perfectly learn to walk, the reward for tracking linear velocity did show a rise | It is better to use the same config and then train for like 10k steps, let us see what happens, what can also be done is fine tuning, but then no, let's put from start no? |
| 2026-02-22_16-28-11 | mjlab-rumi-locomotion | 2026-02-22 | ❌ **Failed** | Previous run, 8Hz natural freq, rounded off, 10k steps, also turned off curriculum in the `rumi_flat_env_cfg` | Looking at outputs, escpecially for like 8000.pt, the robot did learn to walk, this can be said confidently. Also turning on the track linear velocity graph in viser also does prove the same, the graph does oscially but it does hit the max reward too. Now, comparing the graphs with the baseline(kp 10, kd 0, `2026-02-22_01-37-29`) run, it can be definitely said that this run also did not learn to track linear velocity as much as the baseline does. Also this run did encounter NaN around 8k.5-9k steps and did quit. Also post 6k steps, it is pretty evident that the policy started being exploratory, leading to increase in entorpy which could have resulted in actions that are proving to be bad as there are dips in the reward graphs and the training does not seem to be stable post that | Hmmmm, now that it is walking we can term it as success too. Now try spending time to MuJoCo docs carefully and then also debug the problem in getup where |
| 2026-03-21_01-52-27 | mtp-rumi-loco | 2026-03-21 | ❌ **Failed** | - | - | - |
| 2026-03-30_03-55-59 | mtp-rumi-loco | 2026-03-30 | ✅ **Completed** | System identification was carried out successfully and then getup task was also trained successfully and deployed in real life. Hence it is time to test and train rumi on velocity task. This is the first set of run, let me see what kind of errors I do get. There could be joint range mismatch, which is created from the offset for this task and the older joint range for each joint, but ok, it can be seen later. | The robot is walking cleanly. It is also able to track both lin vel and ang vel properly, the arrows do match. Good run, clean run. Check all the observation, action space, init state and all other param once, once done, try transferring this to real robot like getup | Sim2real was performed directly first, and it failed terribly, later open loop sim2real was performed which revealed a lot of information. First things first, the robot is being spawned in the height of 0.25m where it is hard for the hind motors to balance the robot itself prompting it to fail/sit soon. This proved that spawn height has to be changed and retrained. Also since Kp is too high, the steps seem to be too strong, probably we can slowly increase the penalty for soft_landing ensuring that robot does walk soft. Also very important thing is that the robot does not seem to actually move at lower target vel, this should also be noted. What is best is that, spawn the bare xml in mujoco, ensure a proper starting position at the height of 0.25m, use it as the init state and go forward. One last thing, the policies were trained with a maximum torque of 6Nm, but the rated torque(continous) of Dynamixel MX64 is only around 3-4, so it should we worth keeping this for future. Something that has not been touched yet, the velocity sampling ranges. For now the robot is being trained on by sampoing truly real number random interval. This can be changed and I believe should be changed. Let us samping velocities by round them to the first decimal. For the next run perform the following changes: change the init height and joint pos, change the sampling interval by rounding the values off to one decimal place. |
| 2026-04-03_02-06-53 | mtp-rumi-loco | 2026-04-03 | ✅ **Completed** | The spawn height and the spawn state of the robot has been changed to match real life expectation. The velocity will now be sampled discretely by rounding off to 1 decimal(this much accuracy, i would be happy with). Now the initial training starts by sampling all vels (lx, ly, az) in the range under mod 0.5. To also embrace smoother contact with ground due to stiff joints, this run also has reward curriculum for `soft_landing` term that will increase the penalty every 6k runs. | The run was successful and converged much faster. Probably due to sampling at smaller domain (-0.5 to 0.5 of values). The `track_lin_vel` reward now hits a new maximum of greater than 1.8 (weight is 2.0) compared to earlier runs after rounding the sampling values. Sim2real transfer was also successful. One very important thing to note is that, earlier sim2real could have failed because of the fact that observation terms were in a different order than required, so yeah. Coming to the soft_landing part, there seems to be no recovery or improvement in that domain which could indicate that this could be the most soft way the legs could touch the ground?! Or it could also mean that the weight for this penalty is not sufficient | Investigate formally if the runs will converge faster because of rounding off. |
| 2026-04-04_18-13-32 | mtp-rumi-loco | 2026-04-04 | ✅ **Completed** | To have a fair comparison of training time convergence, this run uses the same parameters as `2026-03-30_03-55-59` (default vel sampling ranges, -1 to 1) but then the vel target is rounded off to 1 decimal | The training kind of converged around 300 steps earlier. The vertical offset to the lower sampling run still remains the same which means the robot cannot achieve some velocities that are above 0.5 but below 1.0 in magnitude. | We need to investigate about which velocities cannot be achieved but before that we have to also compare the run with the new smaller sample of vel (-0.5 to 0.5) comparing it with the run `2026-04-03_02-06-53` |
| 2026-04-04_20-22-20 | mtp-rumi-loco | 2026-04-04 | ✅ **Completed** | To have a fair comparison of training time convergence, this run uses the same parameters as `2026-04-03_02-06-53` (new vel sampling ranges -0.5 to 0.5) but then the vel target is purely random sample without rounding off | The training kind of converged around 40 steps earlier. This is opposite of the effect we saw in the larger sampling. We have to profile and conduct this experiment wisely. | Create a comprehensive experimentation with log table that clearly document the changes and the graphs corresponding to each. For now, to understand above which velocities tracking is hard, start sampling from lower ranges and then move to higher ranges. Use the rounding off to 1 place for now, later you can move finer. Design vel curriculum, increase plus 0.2 on each region, that is the best. |
| 2026-04-05_01-35-01 | mtp-rumi-loco | 2026-04-05 | ✅ **Completed** | Created a vel curriculum experiment with target rounded off to 1 decimal place to understand above which velocities linear and vel breaks. | I did run checkpoints for each 1000 steps, the robot seems to walk very stably, but still it is not much clear about where it fails to track velocity. There are some clear drop in `track_lin_vel` reward graph. Should be investigated on why this drop happens. | It is also worthy to run experiments where we run individual experiments for lin_vel_x, lin_vel_y, ang_vel_z and find the invidual limits. Also try to perform sim2real and observe how good it is or not. |
| 2026-04-06_20-23-50 | mtp-rumi-loco | 2026-04-06 | ✅ **Completed** | Performing set of experiments with individual lin_vel_x, lin_vel_y, ang_vel_z. For this experiment, we use the discrete VelocityCommandCfg spaces with ranges between (-0.5, 0.5) only for lin_vel_x. This will also give us a clear idea to understand if we can move at lower speeds too. One very important thing here is that, try to play with lowest commands too. Note, this was trained with the old standing pose the (0.0705 one) | The robot seems to walk all good in simulation, I can still see that the robot is not able to track 0.1m/s target. Sim2real was performed, the 0 input stayed intact, when given 0.1, almost same, when given 0.3, the front legs where able to walk properly for now, the robot does not seem to walk that naturally at lower speeds yet. A very important observation is that while walking, for like 0.2-0.5, the range where it walks, the robot lifts its feet only to a very low height!!! | Investigate what is happening, why is it not able to walk properly in low velocities and also perform proper successful sim2real. The openloop did succeed because the policy was clean. I think in closed loop for a lot of part the robot is strugging because of the fact that noise injected into the sim does not match with the noise profile of actually what the MDP sees in the real robot(take it in a way where it might see more outliers than what it sees in the sim). Maybe deploying the robot in bed kinda structure is failing because the height at which the robot tries to lift its leg is exactly the height to which the bed softens up. |
| 2026-04-08_02-28-38 | mtp-rumi-loco | 2026-04-08 | ✅ **Completed** | I have set a new standing position ensuring it is in the height of 0.25m and then the hind legs are a bit behind and the legs in forward are stretched. This should maintain the COM very well. Now we perform the discrete, lower range sampling runs to check if we are able to perform sim2real. Hope it goes well :) | - | - |
| 2026-04-08_23-48-26 | mtp-rumi-loco | 2026-04-08 | ✅ **Completed** | In the old standing position, I am just trying to increase the range of `encoder_bias` from error of 0.015rad tp 0.1 rad, add `randomize_kp_gains` gains from 15 to 25. Also clipped the torque limit of the physical robot from 6Nm to 4Nm. | - | - |
| 2026-04-09_01-32-32 | mtp-rumi-loco | 2026-04-09 | ✅ **Completed** | ? | - | - |
| 2026-04-10_00-53-33 | mtp-rumi-loco | 2026-04-10 | ✅ **Completed** | Successful sim2real was performed from run `2026-04-06_20-23-50` which was trained with `encoder_bias` of 0.015rad, no `randomize_pd_gains`. All I am trying to do is that with the same old stand height and pose (0.25, 0.0705 one), I am just trying to increase the encoder bias from 0.015 to 0.05rads. I have also changed the `EFFORT_LIMIT` to 4Nm from earlier 6Nm since there is a known hardware limitation. Note, I am still NOT randomising pd gains. | I tried directly performing sim2real on the robot and there are a lot of the trails in which it failed. So falling back to the sim: The first nuance for this run is that, at pure 0 target,  can clearly observe that there are some robot that are moving slowly and some are falling off. Also on observing very closely, there are some robots that is moving a little bit forward and with the lifted hind stance, it is trying to stand. But this is not looking nice. Now upon giving some target to follow, something of 0.3 or 0.5 linear vel, it can be observed that a lot of robots falls to the right side and fails and those robots that are actually walking seems to have lifted hind leg stance. This is surely because either limiting the effort to 4Nm or increasing the encoder bias, the robot now is struggling to stand with the increased error. It's much better to have curriculum based encoder bias so that it would be much easier for us to also observe the breaking point. | Design two runs 1. repeat `2026-04-06_20-23-50` with `EFFORT_LIMIT` of 4Nm. 2. starting with an encoder bias of 0.015 and going all the way by increasing in a fixed pattern for every run. This will also teach us and help us observe what is the optimal bias in which we can realistically train. |
| 2026-04-10_23-20-08 | mtp-rumi-loco | 2026-04-10 | ✅ **Completed** | Same as previous but added a manual weight in the backside of the robot. | When the zero velocity is provided, the robot has learned to stand straight, balancing off the sitting posture of the robot when it is normally spawned with weights. And it was able to walk for velocities too. The only drawback is that, the robot is having an induced ang vel toward the right hand side, and hence when lin_vel is given, the robot does keep on deviating from the straight path, given good amount of time, it completed a circle itself, lol. It is also ensured that zero vel balancing is genuinely what the policy learnt because when I spawn the robot with agent zero, I can observe that the robot sits down a bit. | - |
| 2026-04-11_01-09-17 | mtp-rumi-loco | 2026-04-11 | ❌ **Failed** | Re-running `2026-04-10_00-53-33` but instead of `BuiltInPositionActuator`, we use `IdealPdActuator`. Currently this does the same what MuJoCo does behind in its API. It blindly calculates torque using `tau = kp * error + kd * error_dot` and then clips the torque based on the `EFFORT_LIMITS`. If this works here, then for sure we can try to kind of profile and see how it is breaking in real life based on current, calculating the torque using `K_t`. | - | It has been noted that joint damping has not been applied via the `IdealPdActuator`. Hence re-run this again! |
| 2026-04-11_01-39-22 | mtp-rumi-loco | 2026-04-11 | ✅ **Completed** | Re-running `2026-04-10_00-53-33` but instead of `BuiltInPositionActuator`, we use `IdealPdActuator`. Currently this does the same what MuJoCo does behind in its API. It blindly calculates torque using `tau = kp * error + kd * error_dot` and then clips the torque based on the `EFFORT_LIMITS`. If this works here, then for sure we can try to kind of profile and see how it is breaking in real life based on current, calculating the torque using `K_t`. | Technically if the internal MuJoCo does the same as the IdealPdActuator logic, then the graphs must match, it does not. But then I did remember, I add an weight to the body, this is causing the change of graph behaviour. | Re-run the current experimenting by commenting out the weight code. Lol bro, focus on the work, forget SideMen Among Us for sometime |
| 2026-04-11_03-22-48 | mtp-rumi-loco | 2026-04-11 | ✅ **Completed** | Re-running `2026-04-10_00-53-33` but instead of `BuiltInPositionActuator`, we use `IdealPdActuator`. Currently this does the same what MuJoCo does behind in its API. It blindly calculates torque using `tau = kp * error + kd * error_dot` and then clips the torque based on the `EFFORT_LIMITS`. If this works here, then for sure we can try to kind of profile and see how it is breaking in real life based on current, calculating the torque using `K_t`. | Although, we have gotten so called good looking decent graphs, what has happened is that the robot seems to be strugging to provide torque in the opposite direction, now because of this, the robot seems to move forward for sometime and then lock itself and fall off. This is a very clear indication of the fact that torque in the opposite direction is not being able to calculated in a proper manner. The run was thought to be a success only because the `track_lin_vel` graphs matched but what should be noted is that even though that does match (since before falling off the robot is able to move forward to match with that velocity), what differs drastically is the `Episode_Termination/fell_over` falls off a lots of times compared to the previous successful BuiltinJointPosition runs such as `2026-04-06_20-23-50` | - |
| 2026-04-12_02-01-38 | mtp-rumi-loco | 2026-04-12 | ✅ **Completed** | I have now modeled the torque limit based on the equations $\tau = \tau_{max} * (1 - \frac{abs(\omega)}{\omega_{max}})$ and then clamping it to zero and using it on both the sides. This should be more realistic to model. This is being ran on `EFFORT_LIMIT` on 4Nm. | The curve almost matched with the run `2026-04-10_00-53-33`, for its peak and the profile is somewhat closer to the run `2026-04-06_20-23-50`. But did not translate to any learned walking because the robot continuously falls down when evalutated. It can be clearly observed that when the robot starts moving, after some time it stops and falls down, this almost seems as if the robot is not able to provide negative torque to the joints. This could have been avoided if you has observed the output of the previous run `2026-04-11_03-22-48`, since that also seems to fail. There is something wrong or to be worked about with the IdealPdActuator. | Now since both the IdealPdActuator runs have failed, its better to take a step back and currently work with BuiltInJointPosition controller and then later debug and implement the same |
| 2026-04-12_17-20-39 | mtp-rumi-loco | 2026-04-12 | ✅ **Completed** | This experiment is the same as the successful `2026-04-06_20-23-50` but the only difference is that now we are having `EFFORT_LIMIT` of 4Nm instead of 6Nm. | Although the run has stopped around 3.2k due to power cut, the robot has actually learned to walk and balance properly. This directly indicates the fact that out of the above stated 2 reasons, definitely lowering the `EFFORT_LIMIT` is not the cause of instability. As a matter of fact, it is actually much better if we try to perform sim2real, and then comment about the behavior of the policy. Sim2real was performed, and the robot seems to be moving much smooth. This could be the latest, best keep this as the base and then try to build up and train policies on the same. | To build up on this, two things have to be done, one to somehow understand how the target vel is tracked and evolved. Especially to find how 0.1m/s can be achieved if it can be and then later be trained on different velocities, which is more of ang vel, higher lin vel etc etc. Also, one thing is yet to be investigated, the rewards and their weights. Also, also, also, try to alter and experiment what happens when you change the height of foot_clearance. Currently, even though some height is defined in env_cfgs, we have set the reward that track this height as zero. Therefore this mean that we are not currently enforcing any `foot_height` |
| 2026-04-13_01-21-17 | mtp-rumi-loco | 2026-04-13 | 🟡 **In Progress** | After succesful latest run of `2026-04-12_17-20-39`, I am introducing `joint_torques_l2` based penalty where we try to penalise the torque itself. Eventhough we use Position Actuator, we can get the data of the torque from `data.actuator_force`. I am starting with -1e-4, with the idea that if all motors draw 4Nm, the penalty would be 0.02 (highly unlikely) but this is just to observe what kind of changes will emerge in the behavior!! | - | - |
| 2026-04-13_01-56-32 | mtp-rumi-loco | 2026-04-13 | 🟡 **In Progress** | After succesful latest run of `2026-04-12_17-20-39`, this is a pure curriculum pushing the limits of what the robot could and would learn. Note, this is without the `joint_torques_l2`. | - | - |
| 2026-04-22_20-57-32 | mtp-rumi-loco | 2026-04-22 | ✅ **Completed** | After multiple sim2real attempts of the robot not being 100% reliable walking, real life IMU scenarios such as spikes and then stale of the IMU data, expanding the run `2026-04-12_17-20-39`. | One very late realisation is that the actions are not clipped to [-1, 1]. So for now, it can be any output, which will be multiplied by action_scale. But yeah, the training was smooth, graph was closer to the intended run but not the exact overlap, the sim was clean. Now transferred to real. Butter, as smooth as a butter, kinda run. Finally, time to lock this as final and then write thesis. see ya !!!! | This run has been trained only for the forward velocities, extend this runs to versatile set of vx, vy, omega targets |
| 2026-05-19_12-51-34 | mtp-rumi-loco | 2026-05-19 | ✅ **Completed** | Extending the already working walking to a larger range of vx, vy and omega walkings. Would see the outputs and decide what to change and what not to. | Success as expected. The robot is able to follow commands for all types of vx, vy and omega. | Its time to update the mjlab to the latest version and then retrain policies and try deploying it yet again. Also post update train policies for running and other activities or other terrains. |
| 2026-05-20_12-06-23 | mtp-rumi-loco | 2026-05-20 | ✅ **Completed** | A2a: stateful freeze noise only (ablation). Drops single-frame dropout (`dropout_prob: 0.05 -> 0`) and gravity-spike (`spike_prob: 0.02 -> 0`) from `ImuFreezeGroupNoiseCfg`; keeps the stateful group-freeze term (`freeze_prob=0.003`, duration `U{1,150}`). All other DR / PPO / reward terms identical to the existing freeze-robust pipeline (`Mjlab-Velocity-Flat-Rumi`). Tests whether the stateful freeze alone carries the contribution. Task: `Mjlab-Velocity-Flat-Rumi-A2a`. Max iter: 6000. Log: `mtp-paper-a2a.log`. | sim: ; real:  | - |
| 2026-05-20_14-10-06 | mtp-rumi-loco | 2026-05-20 | ✅ **Completed** | A2b: stateful freeze + single-frame dropout (ablation). Drops gravity-spike only (`spike_prob: 0.02 -> 0`); keeps stateful freeze and the 5% per-step dropout. Tests the marginal contribution of the gravity-spike term. Task: `Mjlab-Velocity-Flat-Rumi-A2b`. Max iter: 6000. Log: `mtp-paper-a2b.log`. | sim: ; real:  | - |
| 2026-05-20_15-47-47 | mtp-rumi-loco | 2026-05-20 | ✅ **Completed** | A2c: stateful freeze + gravity-spike (ablation). Drops single-frame dropout only (`dropout_prob: 0.05 -> 0`); keeps stateful freeze and the 2% gravity-spike. Tests the marginal contribution of the dropout term. Task: `Mjlab-Velocity-Flat-Rumi-A2c`. Max iter: 6000. Log: `mtp-paper-a2c.log`. | sim: ; real:  | - |
| 2026-05-20_17-24-54 | mtp-rumi-loco | 2026-05-20 | ✅ **Completed** | B1: aggressive standard DR, freeze model disabled. `ImuFreezeGroupNoiseCfg` removed. Standard DR ranges widened: push perturbations 2x, foot friction to [0.1, 1.5], encoder bias ~7x (+/-0.015 -> +/-0.1 rad), CoM offset 2x, IMU per-channel uniform noise 3x (gravity +/-0.15, gyro +/-0.6, accel +/-1.5). Tests whether aggressive standard DR alone reproduces freeze-robust behavior without modeling the temporally-correlated failure structurally. Task: `Mjlab-Velocity-Flat-Rumi-B1`. Max iter: 6000. Log: `mtp-paper-b1.log`. | sim: ; real:  | - |
| 2026-05-20_19-01-44 | mtp-rumi-loco | 2026-05-20 | ✅ **Completed** | C1: uncalibrated actuator, freeze model on. Strips identified actuator params (`armature: 0.01623 -> 0`, `joint_damping: 0.59436 -> 0`, `frictionloss: 0.001 -> 0` in `BuiltinPositionActuatorCfg`); keeps the full freeze noise model and all other DR. Tests whether the chirp-fitted Chapter 4 actuator calibration is necessary for sim-to-real or whether the freeze model alone suffices. Task: `Mjlab-Velocity-Flat-Rumi-C1`. Max iter: 6000. Log: `mtp-paper-c1.log`. | sim: ; real:  | - |
| 2026-05-20_20-46-19 | mtp-rumi-loco | 2026-05-20 | ✅ **Completed** | B0: standard-DR baseline (no freeze model). Adds to the paper-experiment chain after C1 completes via `run_b0_after_chain.sh` sidecar. Removes `ImuFreezeGroupNoiseCfg` entirely; keeps default mjlab velocity-task DR ranges (mass/CoM, foot friction, push perturbations, encoder bias, i.i.d. IMU noise). Trained on the full 3D velocity range (`lin_vel_x, lin_vel_y, ang_vel_z` all in [-0.5, 0.5] with curriculum-disabled flat-terrain config). This is the paper-side paired baseline against the existing `Mjlab-Velocity-Flat-Rumi` (freeze-robust) policy; replaces the thesis-era forward-only baseline for the broader 3D-command hardware evaluation. Task: `Mjlab-Velocity-Flat-Rumi-B0`. Max iter: 6000. Log: `mtp-paper-b0.log`. | sim: ; real:  | - |
| 2026-05-22_20-14-26 | mtp-rumi-loco | 2026-05-22 | ✅ **Completed** | EXP B0: standard-DR baseline (no freeze model). Removes `ImuFreezeGroupNoiseCfg` entirely; keeps default mjlab velocity-task DR ranges (mass/CoM, foot friction, push perturbations, encoder bias, i.i.d. IMU noise). Trained on the full 3D velocity range (`lin_vel_x, lin_vel_y, ang_vel_z` all in [-0.5, 0.5] with curriculum-disabled flat-terrain config). Refer to `exp_b0.log` | sim: ; real:  | - |
| 2026-05-22_21-27-33 | mtp-rumi-loco | 2026-05-22 | ✅ **Completed** | EXP A2a: stateful freeze noise only (ablation). Drops single-frame dropout (`dropout_prob: 0.05 -> 0`) and gravity-spike (`spike_prob: 0.02 -> 0`) from `ImuFreezeGroupNoiseCfg`; keeps the stateful group-freeze term (`freeze_prob=0.003`, duration `U{1,150}`). All other DR / PPO / reward terms identical to the existing freeze-robust pipeline. Tests whether the stateful freeze alone carries the contribution. Refer to `exp_a2a.log` | sim: ; real:  | - |
| 2026-05-22_23-26-21 | mtp-rumi-loco | 2026-05-22 | ✅ **Completed** | EXP A2b: stateful freeze + single-frame dropout (ablation). Drops gravity-spike only (`spike_prob: 0.02 -> 0`); keeps stateful freeze and the 5% per-step dropout. Tests the marginal contribution of the gravity-spike term. Refer to `exp_a2b.log`. | sim: ; real:  | - |
| 2026-05-22_23-41-29 | mtp-rumi-loco | 2026-05-22 | ✅ **Completed** | EXP A2c: stateful freeze + gravity-spike (ablation). Drops single-frame dropout only (`dropout_prob: 0.05 -> 0`); keeps stateful freeze and the 2% gravity-spike. Refer to `exp_a2c.log`. | sim: ; real:  | - |
| 2026-05-23_12-12-37 | mtp-rumi-loco | 2026-05-23 | 🟡 **In Progress** | EXP B1: aggressive standard DR, freeze model disabled. `ImuFreezeGroupNoiseCfg` removed. Standard DR ranges widened: push perturbations 2x, foot friction to [0.1, 1.5], encoder bias ~7x (+/-0.015 -> +/-0.1 rad), CoM offset 2x, IMU per-channel uniform noise 3x (gravity +/-0.15, gyro +/-0.6, accel +/-1.5). Tests whether aggressive standard DR alone reproduces freeze-robust behavior without modeling the temporally-correlated failure structurally. Refer to `exp_b1.log`. | sim: ; real:  | - |
| 2026-05-23_12-18-00 | mtp-rumi-loco | 2026-05-23 | 🟡 **In Progress** | EXP C1: uncalibrated actuator, freeze model on. Strips identified actuator params (`armature: 0.01623 -> 0`, `joint_damping: 0.59436 -> 0`, `frictionloss: 0.001 -> 0` in `BuiltinPositionActuatorCfg`); keeps the full freeze noise model and all other DR. Tests whether the chirp-fitted Chapter 4 actuator calibration is necessary for sim-to-real or whether the freeze model alone suffices. Refer to `exp_c1.log`. | sim: ; real:  | - |
| - | - | - | - | - | - | - |

### Status Legend
- 🟡 **In Progress** - Currently running/active experiment
- ✅ **Completed** - Successfully finished experiment
- ❌ **Failed** - Experiment failed or abandoned


## Rumi Getup
| WANDB ID | WANDB Exp Name | Date | Status | Objective | Key Results | What can be improved? |
|-----|-----|----|-----|---------|-------------|----------|
| 2026-02-28_01-05-42 | mjlab-rumi-getup | 2026-02-28 | ✅ **Completed** | I have trained a getup policy before but it seemed to be inaccurate when tried openloop sim2real. Hence after debugging, the joint limits were proven to be wrong, this run has realistic joint limits for all calves.  | The robot did learn to getup, the back legs are weirdly lifted upwards. Not sure if this is because of the jointlimit of calves or target height | We need to conduct any of the two experiments, either increase target height and see what behaviour emerges or we have to increase the maximum joint limit for calves to let it stretch more. Let us go with the first option. Also keep in mind, we haven't performed sysid and matched params, hence it is not systematically accurate. |
| 2026-02-28_01-05-42 | mjlab-rumi-getup | 2026-02-28 | ✅ **Completed** | I have increased the target height from like 20cm to 30cm, this should technically result in a more reasonable leg structure, ofcourse back legs could be lifted to balance the weight, but let me see what pattern emerges | Learned to standup to bigger heights but the stance is unachieveable, sometimes jumps to achieve the stance. | Definitely have to add foot contact to critic and penalise for no contact during getup, this is a must thing to do. Also on open-loop sim2real it was found to mismatch with reality. Perform sysid and use those params. |
| 2026-03-01_22-39-22 | mjlab-rumi-getup | 2026-03-01 | ✅ **Completed** | So, with the arm, damp, frl mentioned in the BAM(Humanoid) repo as start, I set Kp to 2.5, recorded step and sine waves and performed sysid using CMA-ES. Then with those new params, I switched to Kp 10 and then recorded final arm, damp, frl params. Here's the final code: `armature="0.031" damping="0.794" frictionloss="0.008453"`. One thing to be noted here is that the experiment of sysid was carried out by inverting the robot in both sim and real and hence the effect load is just the leg in air. Now this param with Kp 10 has been used to train standup that too at nearly 30cm | Learned to standup successfully, no jumps whatsoever(without even any contact based rewards or lesser action scale or anything). Tried Open Loop sim2real transfer, the robot in reality actually does not reach to the height as mentioned in simulation. The legs in the back seems to receive more load compared to the legs in the front, there is more deviation there. | Two things, 1. re-run sim2real by adding a feature where you lively record/plot the error, atleast on calf joints first. 2. record this very trajectory and perform sysid, or generate sine trajectory for all motors and perform sysid for this. I just performed the 1. (plot + re-run), found an interesting result. The real atleast settles with the target, doesn't follow it quite well but yeah, but but but, the sim overshoots a lot and settles there, low damping? probably. Now I am left with only one option, perform "2", keep the robot upright in both sim and real, generate step and sinewave trajectories and then do the sysid curve fitting. Just as discussed, do the manual tuning and then sysid one, thats the best way. |
| 2026-03-13_20-44-02 | mtp-rumi-getup | 2026-03-13 | ✅ **Completed** | To ensure that sim2real is possible, realistic height estimation is being introduced. Since there is no practical possibility to get the body height, we use forward kinematics based height estimation in the actor. For now, the critic does not see any ground truth from the simulator MuJoCo. Reward structures are untouched, no base lin and base ang vel and curriculum for height is present. This was done with Kp of 10, armature of 0.031, damping of 0.794, frictionloss of 0.008. | The robot has learned to standup, the height is being achieved in the curriculum ranges. If you notice very carefully, there are instances the robot does not actuate the front right leg much, keeps it in air and then decied to actuate to achieve height. Is it some gimmick to maintain COM and achieve height or wot? Investigate. | Can introduce change to desired pose along with height now, not full area but a very small rotation of pose, should also conduct investigations into reward structures, why and what. Should try sim2real to see if slowing down would be necessary or not, should also investigate if providing contact forces or contact point to critic or penalising losing of contact is necessary during training. |
| 2026-03-25_12-01-28 | mtp-rumi-getup | 2026-03-25 | ✅ **Completed** | The system identification was carried out by printing a load and filling it with sand and a visual sim&real match interface was created to find out what parameters actually fit everything. The latest params are Kp 20, armature 0.02, damping 0.45 and frictionloss 0.01 | The robot has learned to standup, sometimes it lifts the front left leg, contact breaks at points. But it works, can be the initial sim2real | One mistake of this run was that in the xml file, joints where written with the armature, damping and fricitionloss values, this is not the case with mjlab format. Hence rerun the experiments by commenting out this information. |
| 2026-03-25_12-01-28 | mtp-rumi-getup | 2026-03-25 | ✅ **Completed** | Re-run the previous experiment without any joint param values embedded in the xml. | The experiment did succeed. The robot did stand up. There is still an issue of robot lifting one of its leg in the air. But it is unsure about the parameters with which it was trained on. | Sad, but retrain, log the parameters too, meanwhile in the backend, for later runs, add contact also as a form of check ensuring that the robot does not leave the contact from the ground at any point of time. If it succeeds, then go ahead in putting sweep runs while performing sim2real. |
| 2026-03-25_20-14-24 | mtp-rumi-getup | 2026-03-25 | ✅ **Completed** | Re-run the previous experiment without any joint param values embedded in the xml but with logging all the rumi_constant params too (silly mistake, my bad) | The robot has learned to standup, pose can be corrected, foot contact has to be ensured at all times, decent for now. Params also did get successfully backed up. | - |
| 2026-03-28_20-14-24 | mtp-rumi-getup | 2026-03-28 | ✅ **Completed** | SYSTEM IDENTIFICATION HAS BEEN PERFORMED AND RESULTS ARE OUT SUCCESSFULLY. USING THOSE PARAMS FOR THIS RUN. `kp: 20, kd: 0 a: 0.01623 d:0.59436 f:0.001`. I also realised that mjlab by default do not set the joint damping parameter too. Hence changed the util spec and actuator codes to accomodate the same. Running getup exp for this for now, once successful, perform sim2real and once that is also successful, perform sweep and then domain randomization (meaningfully). | The robot did learn to getup but if you notice keenly there are two main issues, one the front left leg lifts off of the ground and two it stands up very very fast  | Sim2real was performed on this and the lifting of leg is costing the robot to not standup. So first add a penalty and enforce contact with ground at all times. After that if it fails again, go for velocity reduction |
| 2026-03-30_01-10-25 | mtp-rumi-getup | 2026-03-30 | ✅ **Completed** | Running prev experiment by enforcing all four feet must always remain in contact while getup. This can be ensured by a foot contact sensor penalty based approach for now. The training was only set for around 1.5k episodes. Let us see if the training speed is also the same because for earlier experiments, rumi had learned to getup by around 500 steps. So, thrice of the required window should not be a problem but let's see | First, the overall Episode_Reward/height graph did not reach to the max value that previous runs did reach and the robot visually seems to have its hips at the back tilted for some weird reason. | First extend the run to nearly 6k, also introduce curriculums much later. If the robot again seems to not learn then we might have to take a new approach |
| 2026-03-30_01-49-28 | mtp-rumi-getup | 2026-03-30 | ✅ **Completed** | Running prev experiment till 6k, with curriculum after 4k | The standup looks clean, there is also variations in height and all of them surely does work. In the sim side, things seems to be perfect. But in reality, the robot's hind legs are finding it hard to lift up probably due to COM shifted towards the back. | One greater thing to be noted is that I did change the loction of IMU but I am not sure if this training did include the change of the site of imu. Okay nevertheless, that wouldn't affect much I suppose(bad assumption and bold one to make). One main reason why the sim does fail could be because of the fact that velocity of the motor might be too high or torque might peak which is causing the hind motor to fail in loads. Ofc, the direct solution is to always record data, plot, visualize and then verify but I did notice one fine quirk in actor observation. In real life, since we do not get the motor velocity directly from the addresses, we do finite-difference based velocity calculation, which need not completely match with the mujoco's `data.qvel`. Hence, explore the `ADDR_PRESENT_VELOCITY` in the DynamixelSDK and then go ahead with it, if it even fails then implement finite-diff as a observation into RL.  The default `ADDR_PRESENT_VELOCITY` was used, that did solve the issue and this is a successful getup policy, implemented on the rumi hardware and has been now flagged as `SIM2REAL-SUCCESS` |
| 2026-04-11_23-35-47 | mtp-rumi-getup | 2026-04-11 | ✅ **Completed** | This was just a test run to see if the robot can learn to lift and balance some extra load too. Two major changes were made, 1. a load towards the rear of the body was attached with a mass of 800g, 2. the `EFFORT_LIMIT` term has been changed to 4Nm to ensure consistency with the physical actuator | The robot has visibly learned to standup by learning forward and the also has ensured that its pose is totally flat. You can almost observe that it stands in its toes, like its edges. Hence you have to ensure that this works if transferred to sim2real or not. But this can be a task for an another boring day! | - |
| - | - | - | - | - | - | - |

### Status Legend
- 🟡 **In Progress** - Currently running/active experiment
- ✅ **Completed** - Successfully finished experiment
- ❌ **Failed** - Experiment failed or abandoned

## General Discussions/checklist
-> To transfer getup from sim2real, we can only use realistic available data for actor, hence if we want policy that can take in target height, there are ways to achieve this.
-> One easy way if to have theta, theta_dot, gravity, gyro, last action in theactor. Critic will see the true base height, base lin vel, contacts etc. Reward is based on true base height but probably it will learn to estimate heights from data. Also its good to stack history here, 3-5 past history
-> Thesis-grade idea is to actually come up with an implicit Fk based estimator or an online height estimator that you can use to actually use to condition the policy on. This small network is regressed on actual height based on observations/